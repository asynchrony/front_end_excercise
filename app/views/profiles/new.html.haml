.createProfile
  %h2 Create a profile

  = form_for @profile, :url => profiles_path, :method => :post do |f|

    %h3 General info
    = label :profiles, :first_name, :class => 'required'
    = f.text_field 'first_name'

    %br
    = label :profiles, :last_name, :class => 'required'
    = f.text_field 'last_name'

    %br
    = label :profiles, :email
    = f.text_field 'email'

    %br
    = label :profiles, :date_of_birth, 'Birthday'
    .birthDay
      = f.date_select :date_of_birth

    %br
    %h3 Gender
    = label :profiles, :gender, 'Male'
    = f.radio_button 'gender', 'm'
    = label :profiles, :gender, 'Female'
    = f.radio_button 'gender', 'f'

    %br
    %h3 Annual Income
    = label :profiles, :income, '< $1'
    = f.radio_button 'income', '1'

    = label :profiles, :income, 'About $1'
    = f.radio_button 'income', '2'

    = label :profiles, :income, '> $1'
    = f.radio_button 'income', '3'

    = f.fields_for :location do |l|
      %br
      = label :location, :street, :class => 'required'
      = l.text_field 'street'

      %br
      = label :location, :city, :class => 'required'
      = l.text_field 'city'

      %br
      = label :location, :state, :class => 'required'
      = l.text_field 'state'

      %br
      = label :location, :zip, :class => 'required'
      = l.text_field 'zip'

      %br
      = label :location, :country, :class => 'required'
      = l.text_field 'country'

    %br
    = f.submit 'Submit'

  %br
  %br
  = link_to 'what this should look like..', '/form.jpg', :target => '_blank'
